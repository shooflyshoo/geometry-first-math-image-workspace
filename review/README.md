# Pilot review page

Run locally in Codespaces or any shell with:

```bash
cd review
python3 -m http.server 8000
```

Then open the forwarded port and visit `index.html`.

The page shows the 20 pilot images beside matched excerpt blocks from the fresh manuscript.
